{ class = "query", data = (container_t){
		(hash_md5_t){ data = "" },
		"\n"
	},
	request = {
		action      = (action_t)"convert_to",
		destination = (fd_t)"stdout",
		format      = (format_t)"native"
}},

{ class = "query", data = (container_t){
		(hash_md5_t){ data = "1234" },
		"\n"
	},
	request = {
		action      = (action_t)"convert_to",
		destination = (fd_t)"stdout",
		format      = (format_t)"native"
}},

{ class = "query", data = (container_t){
		(hash_md5_t){ data = "hello" },
		"\n"
	},
	request = {
		action      = (action_t)"convert_to",
		destination = (fd_t)"stdout",
		format      = (format_t)"native"
}},

{ class => "end" }
